
@PACKAGE_INIT@

SET(PN EventSystem)

# location of installed headers
SET(${PN}_INCLUDE_DIR "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")

SET(${PN}_LIBRARY "")
SET(${PN}_DEFINITIONS USING_${PN})

LIST(APPEND CMAKE_MODULE_PATH "@PACKAGE_VALIDATION_INSTALL_DIR@/cmake")

CHECK_REQUIRED_COMPONENTS(${PN})
